#include <iostream>
#define MAX 1000

using namespace std;

int main()
{
    int N[MAX];
    int start = 0;
    int end;
    int mid;
    int cur;
    int testcase;

    cin >> testcase;
    end = testcase;

    for (int i = 0; i < testcase; i++)
    {
        cin >> cur;
        mid = testcase / 2;
        if (cur > N[mid])
        {
        }
    }
}